// examples/demo.ltl — runnable demo for ltl_cue_lite

import std::io
use ltl_cue_lite as lib

fn main() {
    io::println("ltl_cue_lite demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}